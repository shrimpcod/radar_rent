# Git и GitHub Workflow

Базовые принципы и команды работы с Git и GitHub

## 📋 Содержание

1. [Основные понятия](#основные-понятия)
2. [Когда создавать ветки](#когда-создавать-ветки)
3. [Работа с ветками](#работа-с-ветками)
4. [Базовые команды Git](#базовые-команды-git)
5. [Откат изменений](#откат-изменений)
6. [Слияние веток](#слияние-веток)
7. [Практический пример](#практический-пример)

---

## Основные понятия

### Основные ветки

| Ветка | Назначение |
|--------|------------|
| `main` / `master` | Продуктивная версия, стабильный код |
| `develop` | Версия для разработки (опционально) |
| `feature/*` | Новая функциональность |
| `fix/*` | Исправление багов |
| `refactor/*` | Рефакторинг кода |
| `hotfix/*` | Срочные исправления в проде |

### Структура веток

```
main (продуктивная версия)
  ↑
  └── feature/database-models (текущая работа)
  └── fix/login-bug (исправление бага)
  └── refactor/code-cleanup (рефакторинг)
```

---

## Когда создавать ветки

### ✅ Создавать новую ветку, когда:

1. **Новая фича** — добавляете новую функциональность
   - Пример: `feature/user-authentication`
   - Пример: `feature/lead-management`

2. **Исправление бага** — исправляете ошибку
   - Пример: `fix/login-error`
   - Пример: `fix/database-connection`

3. **Рефакторинг** — улучшаете код без изменения функциональности
   - Пример: `refactor/user-service`
   - Пример: `refactor/api-endpoints`

4. **Hotfix** — срочное исправление в проде
   - Пример: `hotfix/security-patch`

### ❌ НЕ создавать ветку, когда:

- Мелкие правки (опечатки, форматирование)
- Изменения в документации (если не связаны с фичей)
- Изменения в `.gitignore`

---

## Работа с ветками

### Создание новой ветки

```bash
# Создание и переключение на новую ветку
git checkout -b feature/имя-ветки

# Примеры:
git checkout -b feature/database-models
git checkout -b fix/login-bug
git checkout -b refactor/user-service
```

### Переключение между ветками

```bash
# Переключиться на существующую ветку
git checkout имя-ветки

# Пример:
git checkout main
git checkout feature/database-models
```

### Просмотр всех веток

```bash
# Показать все локальные ветки (текущая отмечена *)
git branch

# Показать все ветки (локальные и удалённые)
git branch -a
```

### Удаление ветки

```bash
# Удалить локальную ветку (после слияния)
git branch -d feature/имя-ветки

# Принудительное удаление локальной ветки
git branch -D feature/имя-ветки

# Удалить ветку на GitHub
git push origin --delete feature/имя-ветки
```

---

## Базовые команды Git

### Проверка статуса

```bash
# Показать статус файлов (изменённые, добавленные, не отслеживаемые)
git status
```

### Добавление файлов к коммиту

```bash
# Добавить все изменения
git add .

# Добавить конкретный файл
git add backend/app/models/user.py

# Добавить все файлы в папке
git add backend/app/models/
```

### Создание коммита

```bash
# Создать коммит с сообщением
git commit -m "feat: add user model"

# Создать коммит с подробным описанием
git commit -m "feat: add user authentication

- Added User model with email, login, password
- Implemented password hashing with bcrypt
- Added JWT token generation"
```

### Отправка изменений на GitHub

```bash
# Отправить текущую ветку на GitHub
git push

# Отправить ветку с установкой upstream (первый раз)
git push -u origin feature/имя-ветки

# Пример:
git push -u origin feature/database-models
```

### Получение изменений с GitHub

```bash
# Получить изменения из текущей ветки
git pull

# Получить изменения из всех веток
git fetch

# Получить изменения и слить с текущей веткой
git pull origin main
```

---

## Откат изменений

### Откат последнего коммита (до коммита)

```bash
# Отменить последний коммит, но сохранить изменения
git reset --soft HEAD~1

# Отменить последний коммит и удалить изменения
git reset --hard HEAD~1
```

### Откат изменений в файле (до коммита)

```bash
# Откатить изменения в конкретном файле
git checkout -- backend/app/models/user.py

# Откатить все изменения в текущей директории
git checkout -- .
```

### Откат после отправки на GitHub

#### Вариант 1: Создать новый коммит (отмена)

```bash
# Создать коммит, который отменяет предыдущий
git revert HEAD

# Отменить конкретный коммит
git revert <commit-hash>

# Пример:
git revert abc123def456
```

#### Вариант 2: Force push (опасно!)

⚠️ **Использовать только если вы один работаете с веткой!**

```bash
# Откатить локальную ветку на определённый коммит
git reset --hard <commit-hash>

# Принудительно отправить на GitHub
git push --force

# Пример:
git reset --hard abc123def456
git push --force
```

### Откат к определённому коммиту

```bash
# Создать новую ветку из старого коммита
git checkout -b fix-branch abc123def456

# Или откатить текущую ветку
git reset --hard abc123def456
```

### Просмотр истории коммитов

```bash
# Показать историю коммитов
git log

# Показать историю в одну строку
git log --oneline

# Показать историю с графом веток
git log --graph --oneline --all

# Показать изменения в коммите
git show <commit-hash>
```

---

## Слияние веток

### Слияние через Pull Request (рекомендуется)

1. Отправьте ветку на GitHub:
   ```bash
   git push -u origin feature/имя-ветки
   ```

2. Создайте Pull Request на GitHub:
   - Перейдите на GitHub
   - Нажмите "Compare & pull request"
   - Выберите ветку для слияния
   - Напишите описание изменений
   - Нажмите "Create pull request"

3. После проверки:
   - Нажмите "Merge pull request"
   - Поставьте галочку "Delete branch after merge"

### Слияние через командную строку

```bash
# Переключиться на ветку, в которую хотим слить (обычно main)
git checkout main

# Слить изменения из feature-ветки
git merge feature/имя-ветки

# Отправить изменения на GitHub
git push origin main

# Удалить слитую ветку
git branch -d feature/имя-ветки
git push origin --delete feature/имя-ветки
```

### Разрешение конфликтов при слиянии

Если возник конфликт:

1. Git покажет conflicted файлы:
   ```
   CONFLICT (content): Merge conflict in backend/app/models/user.py
   ```

2. Откройте файл и найдите конфликты:
   ```python
   <<<<<<< HEAD
   def old_function():
       return "old"
   =======
   def new_function():
       return "new"
   >>>>>>> feature/new-feature
   ```

3. Разрешите конфликт (оставьте нужный код):
   ```python
   def new_function():
       return "new"
   ```

4. Добавьте и закоммитьте:
   ```bash
   git add backend/app/models/user.py
   git commit -m "resolve merge conflict"
   ```

---

## Практический пример

### Сценарий: Добавление новой модели в БД

```bash
# 1. Создаём новую ветку
git checkout -b feature/database-models

# 2. Работаем с кодом...
# Создаём файлы моделей, настраиваем Alembic и т.д.

# 3. Проверяем статус
git status

# 4. Добавляем изменения
git add backend/app/models/
git add backend/alembic/
git add backend/alembic.ini

# 5. Создаём коммит
git commit -m "feat: add database models and configure alembic

- Added User, Agency, Team models
- Configured Alembic for async PostgreSQL
- Added initial migration"

# 6. Отправляем на GitHub
git push -u origin feature/database-models

# 7. Создаём Pull Request на GitHub
# (через веб-интерфейс)

# 8. После проверки мёржим PR
# (через веб-интерфейс)

# 9. Удаляем ветку
git checkout main
git pull origin main
git branch -d feature/database-models
git push origin --delete feature/database-models
```

### Сценарий: Исправление бага

```bash
# 1. Создаём ветку для исправления
git checkout -b fix/login-error

# 2. Исправляем баг...
# Редактируем файл backend/app/api/auth.py

# 3. Тестируем исправление
# ...

# 4. Коммитим
git add backend/app/api/auth.py
git commit -m "fix: resolve login error with invalid credentials"

# 5. Отправляем
git push -u origin fix/login-error

# 6. Создаём PR и мёржим
# (через веб-интерфейс)
```

---

## Полезные советы

### ✅ Правила хорошего тона

1. **Коммиты должны быть атомарными** — один коммит = одна логическая единица изменений
2. **Сообщения коммитов должны быть понятными** — используйте Conventional Commits
3. **Не коммитьте мусор** — `.pyc`, `__pycache__`, `.env` и т.д.
4. **Пушьте часто** — не держите изменения локально слишком долго
5. **Используйте Pull Requests** — это позволяет провести code review

### ❌ Чего избегать

1. **Не делайте force push в main** — это может удалить чужие изменения
2. **Не работайте в main** — всегда создавайте ветки для новых фич
3. **Не коммитьте сломанный код** — сначала протестируйте
4. **Не пишите длинные сообщения в одну строку** — используйте многострочный формат

### 🔧 Полезные алиасы (добавьте в ~/.gitconfig)

```ini
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    lg = log --graph --oneline --all
    unstage = reset HEAD --
    last = log -1 HEAD
    amend = commit --amend
```

Теперь можно использовать короткие команды:
```bash
git st          # вместо git status
git co main      # вместо git checkout main
git br           # вместо git branch
```

---

## Дополнительные ресурсы

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Интерактивный туториал по Git](https://learngitbranching.js.org/)
